# Safety and Security

This project can command a moving robot. Test the command protocol and watchdog with wheels raised before floor operation. Keep a physical power disconnect accessible and never rely on software stop alone for safety-critical use.

Do not commit serial-device identifiers that contain personal data, API keys, or credentials.
